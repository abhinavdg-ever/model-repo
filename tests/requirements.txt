# Dependencies for the test suite only.
#
#     python -m pytest tests/ -q      -> 172 passed
#
# The tests import the real pipeline and review-ui modules, so they need
# whatever those import AT MODULE LEVEL — which is more than pytest. They do
# NOT need the heavy OCR/ML stack (rapidocr-onnxruntime, opencv, scikit-learn,
# torch): those are imported lazily inside functions and no test reaches them.
#
# Because rapidocr-onnxruntime is absent here, this file also installs cleanly
# on Python 3.13+, unlike core-pipeline/requirements.txt which caps at <3.13.
# Use 3.12 anyway if you intend to run the pipeline itself.
#
# No database is required.

pytest>=8.0

# Imported at module level by core-pipeline and review-ui/backend
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12
python-dotenv>=1.0
psycopg[binary]>=3.2
psycopg_pool>=3.2
Pillow>=10.0
pytesseract>=0.3.10
huggingface_hub>=0.23
azure-storage-blob>=12.20
azure-identity>=1.17
