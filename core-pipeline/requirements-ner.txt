# Member-verification NER layer (GLiNER) — OPTIONAL, install on top of
# requirements.txt:
#
#     pip install -r requirements.txt -r requirements-ner.txt
#
# Kept separate because torch alone is ~2.5 GB installed. The base image stays
# small for deployments that run member verification rules-only.
#
# WITHOUT this layer the pipeline still runs: the reference's rule pass does the
# extraction. But `wrong_member_on_page` decides from the names NER reads off a
# page, so no page can be classified wrong_member and NO DOCUMENT CAN EVER BE
# REJECTED. See docs/LOGIC.md.
#
# After installing, download the checkpoints (~2 GB for all three):
#
#     python -m stages.lib.member.extractors.ner_based.model_downloader
#     python -m stages.lib.member.extractors.ner_based.model_downloader --check
#
# then set MEMBER_NER_ENABLED=true.

gliner>=0.2.13
torch>=2.2
transformers>=4.40,<5
huggingface_hub>=0.23
sentencepiece>=0.2
protobuf>=4.25
