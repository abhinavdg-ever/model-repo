# Optional: Docling + Layout final1 + ConvNeXt HW runtime (packages only).
# pip install -r requirements-docling.txt
#
# Weight files are NOT on PyPI — download separately into:
#   models/rapidocr/   (four RapidOCR files — see docs/API.md)
#   models/hw/handwritten_printed_convnext_tiny.pth
#   models/hw/handwritten_printed_convnext_tiny_backup.pth  # optional prior
# Without those files: final1 → rapidocr-onnxruntime; HW → RF .pkl.
docling>=2.0.0
rapidocr>=2.0.0
torch>=2.2
torchvision>=0.17
# MiniLM semantic filter for Final1 section_headers in *_final1.json
# (falls back to lexical matching if this package is absent).
sentence-transformers>=3.0.0
