fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0
psycopg[binary]>=3.2
azure-storage-blob>=12.20
azure-identity>=1.17
azure-ai-documentintelligence>=1.0.0
Pillow>=10.0
pytesseract>=0.3.10
numpy>=1.24
opencv-python-headless>=4.8
scikit-learn>=1.3
joblib>=1.3
rapidocr-onnxruntime>=1.3
openpyxl>=3.1
python-dotenv>=1.0
psycopg_pool>=3.2
openai>=1.40.0

# Optional: member-verification NER (GLiNER). Docker needs WITH_NER=true.
# Local: pip install -r requirements-ner.txt
# Without it, member verification is rules-only and Rejection cannot fire.
#
# Docling packages: Docker installs requirements-docling.txt always.
# Local: pip install -r requirements-docling.txt
# Weight files stay under core-pipeline/models/ (scp / download) — see docs/API.md.

