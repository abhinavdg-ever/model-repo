# Root-level requirements: the ad-hoc Azure OpenAI connectivity check only.
#
#     python3.12 -m venv .venv-check && source .venv-check/bin/activate
#     pip install -r requirements.txt
#     python check_azure_openai.py
#
# This is NOT the pipeline's dependency list. Those live in:
#   core-pipeline/requirements.txt      the eight-stage imaging chain
#   core-pipeline/requirements-ner.txt  the optional GLiNER layer
#   review-ui/backend/requirements.txt  the read-only viewer API
#   tests/requirements.txt              the 160-test suite (no database)
#
# check_azure_openai.py reads no .env and imports nothing from the pipeline,
# so this one package is the whole dependency.

openai>=1.40
